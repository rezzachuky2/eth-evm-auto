name: Update News and README

on:
  schedule:
    - cron: '*/1 * * * *'  # Menjalankan setiap 1 menit
  workflow_dispatch:  # Untuk menjalankan secara manual

jobs:
  update_news:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Run Python Script
        run: |
          python your_script.py
          git config --global user.email "cakidigital2@gmail.com"
          git config --global user.name "rezzachuky2"
          git add timestamp.py latest_news.json
          git commit -m "Update news and timestamp"
          git push
